import { registerRootComponent } from "expo";
import App from "./App";

// registerRootComponent llama a AppRegistry.registerComponent y se encarga
// de usar Expo Go o una build nativa según corresponda.
registerRootComponent(App);
